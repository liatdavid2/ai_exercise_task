CSV TASK (CRITICAL):

- You MUST search specifically for CSV files
- Use:
    glob.glob('data/**/*.csv', recursive=True)
    glob.glob('**/*.csv', recursive=True)

- Combine results and remove duplicates
- Prefer files inside data/
- Exclude files inside:
    output/
    agent/
    __pycache__/
    .git/
    venv/
    .venv/

- Choose the CSV file whose filename and columns best match the task
- If the task explicitly mentions a filename, strongly prefer that exact basename
- Do NOT assume a fixed schema unless the task explicitly names fields or files

- You MUST use csv.DictReader
- You MUST load all rows before computing aggregates

- First inspect:
    - header names
    - sample row values
    - which columns are numeric
    - which columns look categorical
    - which columns look like dates/timestamps

- If the task explicitly mentions field names such as category, date, total, quantity, or unit_price:
    - use those exact columns if they exist in the header

- Use only columns that actually exist
- If multiple candidate columns exist, choose the one that best matches the task wording

- For aggregation tasks:
    - detect numeric columns dynamically
    - but prefer exact task-mentioned columns when present

- For grouping tasks:
    - detect categorical/grouping columns dynamically
    - but prefer exact task-mentioned columns when present

- For filtering tasks:
    - detect filter columns dynamically
    - but prefer exact task-mentioned columns when present

- If the task asks for revenue:
    - prefer total if it exists
    - otherwise use quantity * unit_price if both exist

- If the task asks for all group totals:
    - return ALL groups, not just the maximum

- Returning "No matching file found" without searching for *.csv specifically is INVALID
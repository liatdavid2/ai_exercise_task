CURRENCY / FX TASK (CRITICAL):

- Return a result that clearly mentions conversion to USD or target currency
- Do not return only a raw number

- Discover files dynamically
- Inspect schema first
- Infer the money field and the currency field semantically
- Do not hardcode filenames or column names

- For each row:
    - read values safely
    - strip whitespace
    - normalize currency to uppercase
    - skip empty or invalid rows
    - never crash on malformed input

- If exchange rates are provided by API:
    - fetch and parse them safely
    - infer the correct conversion direction from the API structure
    - convert every valid row
    - accumulate the final total
    - round to 2 decimals

- Prefer skipping bad rows over failing the whole task